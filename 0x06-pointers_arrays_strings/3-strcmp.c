#include "main.h"
#include <string.h>
/**
 * _strcmp - Compares three parameters
 * @s1: first parameter
 * @s2: second parameter
 *
 * Return: Always integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
