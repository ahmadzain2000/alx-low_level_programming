#include <stadio.h>
/**
 * main-case an infinite loop
 * Retrun:0
 */
int main(vaiod)
{
int i ;
printf("Infinite loop incoming :(/n");
i= 0;
/*
 * whil ( i < 10 )
 * {
 * putcher (i);
 * }
 */
printf("Infinite loop avoided! \\o/\n");
 return (0);
}
